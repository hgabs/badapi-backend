{
  "name": "badapi-backend",
  "version": "1.0.0",
  "main": "app.js",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.21.1",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "node-fetch": "^2.6.1",
    "nodemon": "^2.0.7"
  },
  "scripts": {
    "start": "node app.js",
    "dev": "NODE_ENV='development' nodemon app.js",
    "build:ui": "rm -rf public && cd ../badapi-frontend && yarn build && cp -r build ../badapi-backend/public",
    "deploy": "git add . && git commit -m \"build:ui - $(date -u)\" && git push azure"
  }
}
